*** requestLoggedInUser() ----> get the user that now login in type "Account"
*** requestAddProduct(name, company, cost, category(by type category), description, seller) ----> add new product:
if added return 1.
if category was wrong return 2.
*** requestDigest(productId) ----> get an array that contains details of the product which i give productId:
if was wrong return null
***
